// lib/main.dart temporal
import 'package:flutter/material.dart';

void main() => runApp(
    MaterialApp(
      home: Scaffold(
        appBar: AppBar(title: Text('Estructura lista')),
        body: Center(child: Text('¡Arquitectura en capas creada!')),
      ),
    )
);